//bir grpc servisde olmasi gereken temel yapilanma bu dosyadir yani proto dosyasidir....


syntax = "proto3"; //hangi versiyon
//olusturulcak arayuzler bu namespace altinda olacaktir 
option csharp_namespace = "grpcMessageClient";

package greet;

// The greeting service definition.
service Message {
  // Sends a greeting
     rpc SendMessage (stream MessageRequest) returns (stream MessageResponse);
}

// The request message containing the user's name.
message MessageRequest {
  string name = 1;
  string message=2;
}

// The response message containing the greetings.
message MessageResponse {
  string message = 1;
}
